# backup-run scripts

| Script | Purpose |
|--------|---------|
| `backup_extras.sh` | Brew manifests, macOS plists, Chrome HTML, Cursor extras |
| `chrome_bookmarks_to_html.py` | Chrome Bookmarks JSON → Netscape HTML |

Invoked by `bin/backup` after `backup-run --backup-all --skip-git`.
